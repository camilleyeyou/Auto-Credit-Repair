---
phase: 04-letter-generation
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - frontend/app/(protected)/letters/page.tsx
  - frontend/app/(protected)/disputes/page.tsx
  - frontend/app/(protected)/layout.tsx
autonomous: false
requirements:
  - LTR-01
  - LTR-02
  - LTR-03
  - LTR-04
  - LTR-05
  - LTR-06

must_haves:
  truths:
    - "Nav bar includes a Letters link pointing to /letters"
    - "/letters page renders without error and shows all generated letters grouped by bureau"
    - "Each letter card shows: bureau name, creditor name, FCRA section, generated date, Download PDF button, Preview toggle"
    - "Download PDF button fetches a signed URL from getLetterDownloadUrl and triggers a file download"
    - "Preview toggle expands to show the letter HTML rendered inline (dangerouslySetInnerHTML)"
    - "/disputes page has a Generate Letters button that calls the generateLetters action"
    - "Generate Letters button shows loading state while the action runs"
    - "After generateLetters completes, user is redirected to /letters"
    - "When no approved items exist, Generate Letters button is disabled with an explanation"
    - "Letters page shows an empty state message when no letters have been generated yet"
  artifacts:
    - path: "frontend/app/(protected)/letters/page.tsx"
      provides: "/letters page — bureau-grouped letter cards with download and preview"
    - path: "frontend/app/(protected)/disputes/page.tsx"
      provides: "Generate Letters button integrated into existing disputes page"
    - path: "frontend/app/(protected)/layout.tsx"
      provides: "Letters nav link added"
  key_links:
    - from: "frontend/app/(protected)/letters/page.tsx"
      to: "api.letters.listByUser"
      via: "useQuery(api.letters.listByUser)"
    - from: "frontend/app/(protected)/letters/page.tsx"
      to: "api.letters.getLetterDownloadUrl"
      via: "useQuery(api.letters.getLetterDownloadUrl, { letterId })"
    - from: "frontend/app/(protected)/disputes/page.tsx"
      to: "api.letters.generateLetters"
      via: "useAction(api.letters.generateLetters)"
---

<objective>
Build the /letters frontend page and integrate the Generate Letters button into the /disputes page. This is the user-facing layer of Phase 4.

Purpose: Plans 01 and 02 built the full generation pipeline. This plan wires it to the user — a Letters page to view and download, and a Generate Letters button on the disputes page to kick off the batch.

Output:
- frontend/app/(protected)/letters/page.tsx — letter cards grouped by bureau
- frontend/app/(protected)/disputes/page.tsx — Generate Letters button added (existing file modified)
- frontend/app/(protected)/layout.tsx — Letters nav link added (existing file modified)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-letter-generation/04-CONTEXT.md
@.planning/phases/04-letter-generation/04-01-SUMMARY.md
@.planning/phases/04-letter-generation/04-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From frontend/convex/letters.ts (created in Plan 02):
```typescript
// api.letters.generateLetters — public action, no args
// api.letters.listByUser — public query, no args
//   returns: Array<{ _id, disputeItemId, userId, bureau, letterContent, storageId, generatedAt }>
// api.letters.getLetterDownloadUrl — public query, args: { letterId: Id<"dispute_letters"> }
//   returns: string | null  (signed Convex Storage URL)
```

From frontend/convex/disputeItems.ts (listByUser returns):
```typescript
// Each item includes: _id, bureau, creditorName, accountNumberLast4, disputeReason, fcraSection, fcraSectionTitle, status
// status values: "pending_review" | "approved" | "skipped" | "letter_generated" | ...
```

From frontend/app/(protected)/disputes/page.tsx (existing file — read before modifying):
```typescript
// Already has: useQuery(api.disputeItems.listByUser), useMutation for approve/skip
// Already has: Bureau tabs, DisputeItem interface, BUREAU_LABELS
// CTA link for Generate Letters already exists (but as a simple Link, not a button calling the action)
```

From frontend/app/(protected)/layout.tsx (existing nav — add Letters link):
```typescript
// Current nav links: Dashboard, Upload, Disputes, Profile
// Add: <Link href="/letters" ...>Letters</Link>  (after Disputes, before Profile)
```

Convex React hooks to use:
```typescript
import { useQuery, useMutation, useAction } from "convex/react";
import { api } from "@/convex/_generated/api";
import type { Id } from "@/convex/_generated/dataModel";
// useAction for generateLetters (it's an action, not a mutation)
```

Locked decisions from 04-CONTEXT.md:
- D-20: /letters page with letters grouped by bureau
- D-21: Each card shows bureau, creditor name, FCRA section, generated date, Download PDF button
- D-22: "Preview" expands to show letter HTML rendered inline
- D-23: "Download PDF" triggers file download from Convex Storage
- D-24: "Generate Letters" button on /disputes page — batch generates for all approved items without letters
- D-25: After generation, redirect to /letters
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /letters page and nav link</name>
  <files>
    frontend/app/(protected)/letters/page.tsx,
    frontend/app/(protected)/layout.tsx
  </files>
  <read_first>
    - frontend/app/(protected)/layout.tsx — read current content before editing (add Letters link to nav)
    - frontend/app/(protected)/disputes/page.tsx — read for component patterns (useQuery, card layout, bureau tabs style)
    - .planning/phases/04-letter-generation/04-CONTEXT.md — D-20 through D-23 (Letters page UI decisions)
  </read_first>
  <action>
**frontend/app/(protected)/layout.tsx** — add Letters nav link:

Read the file first, then add `<Link href="/letters" className="text-sm text-muted-foreground hover:text-foreground">Letters</Link>` after the Disputes link and before the Profile link. No other changes.

**frontend/app/(protected)/letters/page.tsx** — create new page:

Use the `"use client"` directive. Use shadcn/ui-style Tailwind classes consistent with the rest of the app (card shadows, muted text, border-b, etc. — match the disputes page style).

Structure:
1. `useQuery(api.letters.listByUser)` for reactive letter list
2. Group letters by bureau client-side (same pattern as disputes page groups by bureau)
3. Display a bureau section for each of experian, equifax, transunion — only render sections that have at least one letter
4. Each letter card (per D-21) shows:
   - Bureau name (capitalized: Experian/Equifax/TransUnion)
   - Creditor name — note: the letter record only has the HTML and storageId; creditor name is not directly on the letter record. To get creditor name, you need the associated dispute item. Since we don't have a join query yet, use the bureau as the card title and the generated date as the subtitle. Add a note in a comment: "TODO: join with dispute_items for creditor name in a future iteration." The card must still be useful — show bureau, generated date (formatted), and Download PDF + Preview buttons.
   - Generated date formatted as e.g. "April 3, 2026" using date-fns `format(new Date(letter.generatedAt), "MMMM d, yyyy")`
   - Download PDF button (see below)
   - Preview toggle button

5. Download PDF button behavior (per D-23):
   - On click, call `useQuery(api.letters.getLetterDownloadUrl, { letterId: letter._id })` — but since useQuery is declarative, use a pattern where each card fetches its own download URL reactively, or implement a separate `DownloadButton` component that calls the query when mounted. The simplest pattern: a child component `LetterCard` that calls `useQuery(api.letters.getLetterDownloadUrl, { letterId })` and renders a download link `<a href={url} download={...}>Download PDF</a>` once the URL resolves. This avoids manual fetch-on-click complexity.

6. Preview toggle (per D-22): a "Preview" button that toggles a `useState` boolean. When expanded, render `<div dangerouslySetInnerHTML={{ __html: letter.letterContent }} className="border rounded p-4 mt-2 text-sm max-h-96 overflow-y-auto" />`. The preview is scoped (small font, scrollable) so it doesn't blow out the page layout.

7. Empty state: if no letters, show a centered message: "No letters generated yet. Go to the Disputes page to approve items and generate letters." with a Link to /disputes.

8. Loading state: if useQuery returns undefined (loading), show a simple spinner or "Loading letters..." text.

Page header: `<h1 className="text-2xl font-semibold">Dispute Letters</h1>` with a subtitle "Download and print your bureau dispute letters."
  </action>
  <verify>
    <automated>grep "getLetterDownloadUrl" "/Users/user/Desktop/Credit Repair /frontend/app/(protected)/letters/page.tsx" && grep "letters" "/Users/user/Desktop/Credit Repair /frontend/app/(protected)/layout.tsx" && grep "listByUser" "/Users/user/Desktop/Credit Repair /frontend/app/(protected)/letters/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - grep "letters" frontend/app/(protected)/layout.tsx exits 0  (nav link added)
    - grep "href=\"/letters\"" frontend/app/(protected)/layout.tsx exits 0
    - grep "listByUser" frontend/app/(protected)/letters/page.tsx exits 0
    - grep "getLetterDownloadUrl" frontend/app/(protected)/letters/page.tsx exits 0
    - grep "dangerouslySetInnerHTML" frontend/app/(protected)/letters/page.tsx exits 0  (preview)
    - grep "No letters generated" frontend/app/(protected)/letters/page.tsx exits 0  (empty state)
    - grep "date-fns" frontend/app/(protected)/letters/page.tsx exits 0 OR grep "format" frontend/app/(protected)/letters/page.tsx exits 0  (date formatting)
    - File frontend/app/(protected)/letters/page.tsx exists
  </acceptance_criteria>
  <done>
    /letters page renders letter cards grouped by bureau with download and preview. Nav has Letters link. Empty state is handled. Date formatting uses date-fns.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate Letters button on /disputes page</name>
  <files>frontend/app/(protected)/disputes/page.tsx</files>
  <read_first>
    - frontend/app/(protected)/disputes/page.tsx — read full current content (it has an existing CTA for letters that must be replaced/upgraded)
    - .planning/phases/04-letter-generation/04-CONTEXT.md — D-24 (Generate Letters button), D-25 (redirect to /letters after), D-27 (only generate for approved items)
  </read_first>
  <action>
Read the full current disputes/page.tsx before editing.

The page currently has a "Generate Letters for Approved Items" CTA that links to /letters. Replace this with a proper button that:

1. Calls the generateLetters action using `useAction(api.letters.generateLetters)`.
2. Tracks loading state with `useState<boolean>`.
3. Tracks count of approved items without letters:
   - Count items where status === "approved" from the existing `listByUser` query result (already available in the component)
   - The button is disabled when approvedCount === 0 OR isGenerating === true
4. Button label:
   - Default: "Generate Letters ({approvedCount} approved items)"
   - While running: "Generating letters..."
   - When no approved items: "Generate Letters (no approved items)" — disabled
5. On click handler:
   - Set isGenerating = true
   - Call `await generateLettersAction()`
   - On success: redirect to /letters using `useRouter()` from next/navigation
   - On error: display error message using a simple `useState<string | null>` error state — show below the button if non-null: `<p className="text-sm text-red-600 mt-2">{error}</p>`
   - In finally: set isGenerating = false
6. The existing per-item status update UI (approve/skip buttons) must remain unchanged.
7. Place the Generate Letters button in a visible section — either above the bureau tabs or below the items list, in a `<div className="mt-6 flex flex-col items-start gap-2">`. The existing Link to /letters should be replaced by this new button (remove the old static Link if present).

Add `useRouter` import from "next/navigation" if not already present.
Add `useAction` import from "convex/react" if not already present.
  </action>
  <verify>
    <automated>grep "generateLetters" "/Users/user/Desktop/Credit Repair /frontend/app/(protected)/disputes/page.tsx" && grep "useAction" "/Users/user/Desktop/Credit Repair /frontend/app/(protected)/disputes/page.tsx" && grep "isGenerating" "/Users/user/Desktop/Credit Repair /frontend/app/(protected)/disputes/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - grep "generateLetters" frontend/app/(protected)/disputes/page.tsx exits 0
    - grep "useAction" frontend/app/(protected)/disputes/page.tsx exits 0
    - grep "isGenerating" frontend/app/(protected)/disputes/page.tsx exits 0
    - grep "router.push" frontend/app/(protected)/disputes/page.tsx exits 0  (redirect after generation)
    - grep "approved" frontend/app/(protected)/disputes/page.tsx exits 0  (approved count filtering)
    - Existing approve/skip functionality is unchanged (grep "updateDisputeStatus" still exits 0)
  </acceptance_criteria>
  <done>
    /disputes page has a functional Generate Letters button that shows approved item count, calls generateLetters action, shows loading state, handles errors, and redirects to /letters on success.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 4 letter generation pipeline:
    - FastAPI POST /api/letters/generate: Claude-personalized letter body + WeasyPrint PDF
    - WeasyPrint system deps in Dockerfile
    - Convex schema: dispute_letters table
    - generateLetters action: batch generates letters for all approved items, stores PDFs in Convex Storage
    - /letters page: bureau-grouped letter cards with download and preview
    - Generate Letters button on /disputes page
    - Letters nav link
  </what-built>
  <how-to-verify>
1. Start the frontend: `cd frontend && npm run dev`
2. Start the backend locally: `cd backend && uvicorn main:app --reload`
   - Verify: `curl http://localhost:8000/api/letters/health` returns `{"status":"ok","service":"creditfix-letters"}`
   - If WeasyPrint import fails locally (missing system libs on macOS), install via Homebrew: `brew install pango cairo gdk-pixbuf gobject-introspection` then retry
3. Sign in to the app at http://localhost:3000
4. Navigate to /disputes — confirm the "Generate Letters" button is visible
5. If no approved dispute items exist: click Approve on one or more items first
6. Click "Generate Letters (N approved items)"
   - Confirm the button shows "Generating letters..." loading state
   - After completion, confirm redirect to /letters
7. On the /letters page:
   - Confirm letter cards are visible grouped by bureau
   - Click "Preview" on a card — confirm the letter HTML expands inline with visible text (date, bureau address, body paragraph, signature, enclosures)
   - Click "Download PDF" — confirm a PDF file downloads
   - Open the downloaded PDF — confirm it is readable, formatted as a standard business letter on 8.5x11 paper with 1 inch margins
8. Check the letter body text:
   - Confirm the FCRA section is cited (e.g., "15 U.S.C. § 1681i")
   - Confirm the letter says "USPS Certified Mail"
   - Confirm the signature block says "Sincerely," with blank space and the user's name
   - Confirm "Enclosures: Copy of government-issued ID, Copy of relevant credit report page"
9. If you have two different approved dispute items: generate letters for both and confirm the body paragraphs are NOT identical (per D-18, LTR-06)
10. Navigate to /disputes — confirm the approved items that got letters now show "letter_generated" status
  </how-to-verify>
  <resume-signal>Type "approved" if everything works correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Overall plan verification:
1. `grep "href=\"/letters\"" frontend/app/(protected)/layout.tsx` — exits 0 (nav link)
2. `grep "listByUser" frontend/app/(protected)/letters/page.tsx` — exits 0 (data fetching)
3. `grep "getLetterDownloadUrl" frontend/app/(protected)/letters/page.tsx` — exits 0 (download URL)
4. `grep "dangerouslySetInnerHTML" frontend/app/(protected)/letters/page.tsx` — exits 0 (preview)
5. `grep "generateLetters" frontend/app/(protected)/disputes/page.tsx` — exits 0 (action call)
6. `grep "useAction" frontend/app/(protected)/disputes/page.tsx` — exits 0
7. `grep "router.push" frontend/app/(protected)/disputes/page.tsx` — exits 0 (redirect)
8. Human checkpoint approved
</verification>

<success_criteria>
- /letters page renders letter cards grouped by bureau, shows download + preview per card, handles empty state
- Download PDF triggers file download from Convex Storage signed URL
- Preview expands to show formatted letter HTML inline
- /disputes page has Generate Letters button with loading state, approved item count, error handling, and redirect to /letters on success
- Letters nav link is present in layout
- Dispute items updated to letter_generated status after generation
- Downloaded PDF is print-ready: business letter format, correct bureau address, FCRA citation, user's name/address, signature block, enclosures
</success_criteria>

<output>
After completion, create `.planning/phases/04-letter-generation/04-03-SUMMARY.md` following the summary template at `@$HOME/.claude/get-shit-done/templates/summary.md`
</output>
