---
phase: 01-foundation
plan: "04"
type: execute
wave: 3
depends_on:
  - "01-02"
  - "01-03"
autonomous: false
files_modified:
  - frontend/next.config.ts
  - frontend/.env.local
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "The Next.js frontend is live on Vercel and accessible at a public HTTPS URL"
    - "The FastAPI backend is live on Railway and GET /api/health returns {\"status\": \"ok\", \"service\": \"creditfix-api\"}"
    - "The Vercel deployment uses the production Convex deployment (npx convex deploy was run)"
    - "Visiting the Vercel URL unauthenticated redirects to /signin"
    - "User can sign up on the live Vercel URL and the session persists across browser refresh"
    - "The live frontend /api/health proxy call successfully reaches the Railway backend"
  artifacts:
    - path: "frontend/next.config.ts"
      provides: "Optional rewrites proxy for /api/* to FastAPI (avoids CORS in browser for same-origin calls)"
    - path: ".planning/phases/01-foundation/01-04-SUMMARY.md"
      provides: "Live Vercel URL, live Railway URL, confirmation of end-to-end connection"
  key_links:
    - from: "Vercel deployment"
      to: "Convex production deployment"
      via: "CONVEX_DEPLOY_KEY + build command: npx convex deploy --cmd 'next build'"
      pattern: "CONVEX_DEPLOY_KEY"
    - from: "Vercel deployment"
      to: "Railway backend"
      via: "NEXT_PUBLIC_FASTAPI_URL env var set to Railway public URL"
      pattern: "NEXT_PUBLIC_FASTAPI_URL"
---

<objective>
Deploy the frontend to Vercel and the backend to Railway. Validate the full stack is live and functional: auth works on Vercel, FastAPI health check is reachable on Railway, and the two services can communicate.

Purpose: D-16 requires live deployments in Phase 1 to validate end-to-end connectivity before building on top of the stack. Finding deployment issues now (CORS, Convex production deploy) is far cheaper than discovering them in Phase 3.

Output: Two live URLs — one Vercel, one Railway — with the full auth flow working end-to-end in production.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Prepare production configuration and push to GitHub</name>
  <files>
    frontend/next.config.ts
    .gitignore
  </files>

  <read_first>
    - /Users/user/Desktop/Credit Repair /.planning/phases/01-foundation/01-RESEARCH.md (Pitfall 6: Convex Functions Not Deployed to Production, "Vercel Build Command for Convex Deploy" section, Environment Availability section)
    - /Users/user/Desktop/Credit Repair /frontend/next.config.ts (current state)
    - /Users/user/Desktop/Credit Repair /.gitignore (ensure .env.local and venv/ are excluded)
  </read_first>

  <action>
**Step 1 — Verify .gitignore is correct:**
```bash
cat "/Users/user/Desktop/Credit Repair /.gitignore"
```
Ensure these patterns are present (add any missing):
```
.env.local
venv/
__pycache__/
*.pyc
.next/
node_modules/
```

**Step 2 — Update next.config.ts with optional API proxy rewrites:**
Read the current `frontend/next.config.ts` first, then add rewrites so the frontend can call `/api/*` without browser CORS issues in production:
```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Proxy /api/* requests to FastAPI backend in production.
  // This avoids browser CORS issues on Vercel preview deploys where
  // the FastAPI URL may differ from the configured FRONTEND_URL on Railway.
  async rewrites() {
    const fastapiUrl = process.env.NEXT_PUBLIC_FASTAPI_URL ?? "http://localhost:8000";
    return [
      {
        source: "/api/backend/:path*",
        destination: `${fastapiUrl}/:path*`,
      },
    ];
  },
};

export default nextConfig;
```

This is optional for Phase 1 but sets up a clean proxy pattern for all future phases.

**Step 3 — Initialize git and push to GitHub:**
```bash
cd "/Users/user/Desktop/Credit Repair "
git init
git add -A
# Double-check that .env.local is NOT staged:
git status | grep env.local
# If .env.local appears, run: git rm --cached frontend/.env.local
git commit -m "feat: Phase 1 foundation — Next.js 15, Convex Auth, FastAPI scaffold"
```

Create a GitHub repository named `creditfix` via the GitHub web UI or CLI, then:
```bash
git remote add origin https://github.com/YOUR_USERNAME/creditfix.git
git push -u origin main
```

**Step 4 — Get Convex production deploy key:**
In the Convex dashboard (dashboard.convex.dev):
1. Select your CreditFix project
2. Go to Settings → Deploy Keys
3. Copy the Production deploy key (starts with `prod:`)

This key will be needed in the next task for Vercel environment variables.
  </action>

  <verify>
    <automated>grep -n "NEXT_PUBLIC_FASTAPI_URL\|rewrites\|backend" "/Users/user/Desktop/Credit Repair /frontend/next.config.ts" &amp;&amp; grep "\.env\.local" "/Users/user/Desktop/Credit Repair /.gitignore"</automated>
  </verify>

  <acceptance_criteria>
    - `frontend/next.config.ts` contains `rewrites()` function — grep: `grep "rewrites" frontend/next.config.ts`
    - `.gitignore` excludes `.env.local` — grep: `grep "\.env\.local" .gitignore`
    - `.gitignore` excludes `venv/` — grep: `grep "venv" .gitignore`
    - `git status` shows a clean working tree (all files committed) — run: `git status --short` returns empty or only untracked files that should be ignored
    - `git log --oneline -1` shows a commit message containing "Phase 1 foundation"
  </acceptance_criteria>

  <done>Repository is initialized with a clean commit. .env.local is excluded from git. next.config.ts has the API proxy rewrites. Code is pushed to GitHub.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1 prepared the production build config and pushed code to GitHub. Now the two deployments need to be configured via their dashboards (Railway CLI is not installed; Vercel CLI may not be installed — both services support dashboard-based GitHub integration per RESEARCH.md Environment Availability section).
  </what-built>

  <how-to-verify>
    **Railway deployment (FastAPI backend):**

    1. Go to railway.app → New Project → Deploy from GitHub repo → select `creditfix`
    2. In the service settings, set Root Directory to `backend/`
    3. Railway will auto-detect the Dockerfile — confirm it shows "Dockerfile" as the build method
    4. Add environment variable in Railway: `FRONTEND_URL` = your Vercel URL (you can update this after Vercel deploy; use `http://localhost:3000` for now and update after step 6)
    5. Wait for the deployment to complete — copy the Railway public URL (e.g., `https://creditfix-api-production.up.railway.app`)
    6. Test: `curl https://YOUR_RAILWAY_URL/api/health` — must return `{"status":"ok","service":"creditfix-api"}`

    **Vercel deployment (Next.js frontend):**

    7. Go to vercel.com → Add New Project → import `creditfix` repo
    8. Set Root Directory to `frontend/`
    9. Set Build Command to: `npx convex deploy --cmd 'next build'`
       (This deploys Convex functions to production before building Next.js)
    10. Add these environment variables in Vercel:
        - `CONVEX_DEPLOY_KEY` = the production deploy key copied in Task 1 (starts with `prod:`)
        - `NEXT_PUBLIC_CONVEX_URL` = your production Convex URL (from Convex dashboard → Settings → URL)
        - `NEXT_PUBLIC_FASTAPI_URL` = your Railway public URL from step 5
        - `CONVEX_AUTH_SECRET` = same value as in your local `frontend/.env.local`
    11. Deploy — wait for build to succeed

    **End-to-end validation:**

    12. Visit the Vercel URL — should redirect to /signin (not a blank page or 500 error)
    13. Sign up with a real email and password on the live Vercel URL
    14. Fill in the Profile page (full name + address) and save — confirm "Profile saved successfully."
    15. Refresh the browser — confirm session is still active (no redirect to /signin)
    16. Open browser DevTools → Network → filter for `/api/health` — click the health check call or manually fetch it — confirm 200 response from Railway
    17. Update Railway env var `FRONTEND_URL` to the Vercel production URL (e.g., `https://creditfix.vercel.app`)

    **What to type when done:**
    - "approved" if all 17 steps pass
    - Describe specific failures if something didn't work (include the step number and error message)
  </how-to-verify>

  <resume-signal>Type "approved" if both deployments are live and all 17 validation steps pass. Otherwise describe what failed.</resume-signal>
</task>

</tasks>

<verification>
After human approval:

1. `curl https://YOUR_RAILWAY_URL/api/health` returns `{"status":"ok","service":"creditfix-api"}`
2. Visiting the Vercel URL unauthenticated redirects to `/signin`
3. Full auth flow works on the live Vercel URL (sign up → dashboard → profile save → refresh → still logged in)
4. Railway FRONTEND_URL is set to the Vercel production URL

Record both live URLs in the SUMMARY file.
</verification>

<success_criteria>
- D-16: Deployments are live and the full stack works end-to-end in production
- D-17: Frontend is live on Vercel (HTTPS URL accessible)
- D-18: Backend is live on Railway, GET /api/health returns correct JSON
- D-19: All required environment variables set on both platforms (CONVEX_DEPLOY_KEY, NEXT_PUBLIC_CONVEX_URL, NEXT_PUBLIC_FASTAPI_URL, CONVEX_AUTH_SECRET on Vercel; FRONTEND_URL on Railway)
- AUTH-01/02/03: Full auth flow and profile save work on the live Vercel URL (not just localhost)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md` with:
- Live Vercel URL
- Live Railway URL
- Confirmation that npx convex deploy ran successfully during Vercel build
- All environment variables configured (names only, not values)
- Any CORS or deployment issues encountered and how they were resolved
</output>
